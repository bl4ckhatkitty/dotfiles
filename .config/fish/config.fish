if status is-interactive
    # Commands to run in interactive sessions can go here
end

set fish_greeting ""

# aliases
alias ll="exa -l"
alias ls="exa -l"
alias l="exa -a -l"
alias tree="exa -a -l --icons --tree"
alias rr="ranger"
